programa {
  funcao inicio() {
    real b,al,ar
    escreva("Qual a base do triangulo? ")
    leia(b)
    limpa()
    escreva("Qual a altura do triangulo? ")
    leia(al)
    limpa()
    ar=(b*al)/2
    escreva("A área do triangulo é: "+ar)
    
      }
}
